# Runtime dependencies
fdb==2.0.2
pg8000

# Install:
#   py -m pip install fdb pg8000

# Generate .exe (Windows, run from project root):
#   py -m pip install pyinstaller
#   py -m PyInstaller --onefile --windowed --name="ImportadorSistemaDatha" main.py
